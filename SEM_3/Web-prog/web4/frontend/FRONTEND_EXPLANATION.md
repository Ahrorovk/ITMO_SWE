# Подробное объяснение фронтенда Web4 (Angular)

## 📁 Структура проекта

Проект использует Angular 17 с standalone компонентами (без NgModules):
- **Models** - TypeScript интерфейсы для типизации данных
- **Services** - сервисы для работы с API
- **Components** - компоненты UI (Login, Main)
- **Guards** - защита маршрутов
- **Interceptors** - перехватчики HTTP запросов

---

## 🚀 1. main.ts - Точка входа приложения

```typescript
bootstrapApplication(AppComponent, {
  providers: [
    provideRouter(routes),
    provideHttpClient(withInterceptorsFromDi()),
    provideAnimations(),
    {
      provide: HTTP_INTERCEPTORS,
      useClass: AuthInterceptor,
      multi: true
    }
  ]
})
```

### Функции и импорты:

**`bootstrapApplication()`**
- Запускает Angular приложение
- Первый параметр: корневой компонент (`AppComponent`)
- Второй параметр: конфигурация провайдеров

**Провайдеры:**

1. **`provideRouter(routes)`**
   - Настраивает маршрутизацию
   - `routes` импортируется из `app.routes.ts`

2. **`provideHttpClient(withInterceptorsFromDi())`**
   - Настраивает HTTP клиент для запросов к API
   - `withInterceptorsFromDi()` - позволяет использовать функциональные интерцепторы

3. **`provideAnimations()`**
   - Включает анимации Angular (для PrimeNG компонентов)

4. **`HTTP_INTERCEPTORS`**
   - Регистрирует `AuthInterceptor` для перехвата всех HTTP запросов
   - `multi: true` - позволяет иметь несколько интерцепторов

---

## 🎯 2. app.component.ts - Корневой компонент

```typescript
@Component({
  selector: 'app-root',
  standalone: true,
  imports: [RouterOutlet],
  template: '<router-outlet></router-outlet>'
})
export class AppComponent {
  title = 'web4-frontend';
}
```

### Аннотации:

- **`@Component`** - декоратор, который превращает класс в Angular компонент
  - **`selector: 'app-root'`** - селектор для использования в HTML (`<app-root></app-root>`)
  - **`standalone: true`** - компонент не требует NgModule (новый подход Angular 14+)
  - **`imports: [RouterOutlet]`** - импортирует `RouterOutlet` для отображения маршрутов
  - **`template`** - HTML шаблон компонента (inline)

### Класс:

- **`AppComponent`** - корневой компонент приложения
- **`title`** - свойство компонента (не используется в шаблоне)

### Что делает:

- Отображает `<router-outlet>` - место, где Angular будет рендерить компоненты маршрутов

---

## 🗺️ 3. app.routes.ts - Конфигурация маршрутов

```typescript
export const routes: Routes = [
  { path: '', redirectTo: '/login', pathMatch: 'full' },
  { path: 'login', component: LoginComponent },
  { path: 'main', component: MainComponent, canActivate: [authGuard] },
  { path: '**', redirectTo: '/login' }
];
```

### Тип:

- **`Routes`** - массив объектов конфигурации маршрутов

### Маршруты:

1. **`{ path: '', redirectTo: '/login', pathMatch: 'full' }`**
   - Корневой путь (`/`) перенаправляет на `/login`
   - **`pathMatch: 'full'`** - точное совпадение (не частичное)

2. **`{ path: 'login', component: LoginComponent }`**
   - При переходе на `/login` отображается `LoginComponent`

3. **`{ path: 'main', component: MainComponent, canActivate: [authGuard] }`**
   - При переходе на `/main` отображается `MainComponent`
   - **`canActivate: [authGuard]`** - перед активацией маршрута проверяется guard
   - Если guard вернет `false` - пользователь будет перенаправлен на `/login`

4. **`{ path: '**', redirectTo: '/login' }`**
   - Wildcard маршрут - любой несуществующий путь перенаправляет на `/login`

---

## 📦 4. MODELS - TypeScript интерфейсы

### 4.1. auth.model.ts - Модели для аутентификации

```typescript
export interface LoginRequest {
  username: string;
  password: string;
}

export interface RegisterRequest {
  username: string;
  password: string;
  fullName: string;
  groupNumber: string;
  variant: number;
}

export interface AuthResponse {
  token: string;
  username: string;
  fullName: string;
  groupNumber: string;
  variant: number;
}
```

#### Интерфейсы:

- **`interface`** - TypeScript конструкция для описания структуры объекта
- **`export`** - делает интерфейс доступным для импорта в других файлах

#### Назначение:

- **`LoginRequest`** - данные для входа (отправляются на сервер)
- **`RegisterRequest`** - данные для регистрации (отправляются на сервер)
- **`AuthResponse`** - ответ сервера при успешной аутентификации

---

### 4.2. result.model.ts - Модели для результатов

```typescript
export interface PointRequest {
  x: number;
  y: number;
  r: number;
}

export interface ResultResponse {
  id: number;
  x: number;
  y: number;
  r: number;
  result: boolean;
  time: string;
  bench: number;
}
```

#### Назначение:

- **`PointRequest`** - координаты точки для проверки
- **`ResultResponse`** - результат проверки точки с сервера

---

## 🔧 5. SERVICES - Сервисы для работы с API

### 5.1. auth.service.ts - Сервис аутентификации

```typescript
@Injectable({
  providedIn: 'root'
})
export class AuthService {
  private apiUrl = 'http://localhost:8080/api/auth';
  private tokenKey = 'auth_token';
  private userKey = 'user_info';

  constructor(private http: HttpClient) {}
}
```

#### Аннотации:

- **`@Injectable({ providedIn: 'root' })`**
  - Делает класс инжектируемым (можно использовать в конструкторах)
  - **`providedIn: 'root'`** - создает один экземпляр на все приложение (Singleton)

#### Свойства:

- **`apiUrl`** - базовый URL API для аутентификации
- **`tokenKey`** - ключ для хранения токена в localStorage
- **`userKey`** - ключ для хранения данных пользователя в localStorage

#### Методы:

**`login(request: LoginRequest): Observable<AuthResponse>`**
- Отправляет POST запрос на `/api/auth/login`
- **`Observable<AuthResponse>`** - RxJS Observable (асинхронный поток данных)
- **`.pipe(tap(...))`** - оператор RxJS для побочных эффектов
- Вызывает `setAuthData()` для сохранения токена

**`register(request: RegisterRequest): Observable<AuthResponse>`**
- Отправляет POST запрос на `/api/auth/register`
- Аналогично `login()`, но для регистрации

**`logout(): void`**
- Удаляет токен и данные пользователя из localStorage
- Вызывается при выходе

**`getToken(): string | null`**
- Получает токен из localStorage
- Возвращает `string` или `null` если токена нет

**`getUser(): AuthResponse | null`**
- Получает данные пользователя из localStorage
- Парсит JSON строку в объект

**`isAuthenticated(): boolean`**
- Проверяет, есть ли токен
- **`!!`** - двойное отрицание для преобразования в boolean

**`setAuthData(response: AuthResponse): void` - ПРИВАТНЫЙ**
- Сохраняет токен и данные пользователя в localStorage
- Проверяет валидность ответа
- Верифицирует сохранение токена

---

### 5.2. result.service.ts - Сервис для работы с результатами

```typescript
@Injectable({
  providedIn: 'root'
})
export class ResultService {
  private apiUrl = 'http://localhost:8080/api/results';

  constructor(private http: HttpClient) {}
}
```

#### Методы:

**`checkPoint(request: PointRequest): Observable<ResultResponse>`**
- Отправляет POST запрос на `/api/results/check`
- Проверяет попадание точки в область

**`getResults(): Observable<ResultResponse[]>`**
- Отправляет GET запрос на `/api/results`
- Получает все результаты текущего пользователя

**`clearResults(): Observable<void>`**
- Отправляет DELETE запрос на `/api/results`
- Удаляет все результаты пользователя

---

## 🛡️ 6. GUARDS - Защита маршрутов

### 6.1. auth.guard.ts - Guard для проверки аутентификации

```typescript
export const authGuard: CanActivateFn = (route, state) => {
  const authService = inject(AuthService);
  const router = inject(Router);

  if (authService.isAuthenticated()) {
    return true;
  }

  router.navigate(['/login']);
  return false;
};
```

#### Тип:

- **`CanActivateFn`** - функциональный guard (новый подход Angular 15+)
- Функция принимает `route` и `state`, возвращает `boolean` или `UrlTree`

#### Функции:

- **`inject(AuthService)`** - получает экземпляр сервиса через dependency injection
- **`inject(Router)`** - получает экземпляр роутера

#### Логика:

1. Проверяет, аутентифицирован ли пользователь через `authService.isAuthenticated()`
2. Если да - возвращает `true` (разрешает доступ к маршруту)
3. Если нет - перенаправляет на `/login` и возвращает `false`

#### Использование:

- Применяется в `app.routes.ts` к маршруту `/main`
- Вызывается перед активацией маршрута

---

## 🔄 7. INTERCEPTORS - Перехватчики HTTP запросов

### 7.1. auth.interceptor.ts - Интерцептор для добавления JWT токена

```typescript
@Injectable()
export class AuthInterceptor implements HttpInterceptor {
  constructor(
    private authService: AuthService,
    private router: Router
  ) {}

  intercept(req: HttpRequest<any>, next: HttpHandler): Observable<HttpEvent<any>> {
    // ...
  }
}
```

#### Интерфейс:

- **`HttpInterceptor`** - интерфейс Angular для перехвата HTTP запросов
- Должен реализовать метод `intercept()`

#### Метод `intercept()`:

**Параметры:**
- **`req: HttpRequest<any>`** - текущий HTTP запрос
- **`next: HttpHandler`** - следующий обработчик в цепочке

**Возвращает:**
- **`Observable<HttpEvent<any>>`** - поток событий HTTP

**Логика:**

1. **Получение токена:**
   ```typescript
   const token = this.authService.getToken();
   ```

2. **Добавление токена в заголовок:**
   ```typescript
   if (token) {
     req = req.clone({
       setHeaders: {
         Authorization: `Bearer ${token}`
       }
     });
   }
   ```
   - **`req.clone()`** - создает копию запроса с новыми заголовками
   - Добавляет заголовок `Authorization: Bearer <token>`

3. **Обработка ошибок:**
   ```typescript
   return next.handle(req).pipe(
     catchError((error: HttpErrorResponse) => {
       if ((error.status === 401 || error.status === 403) && !req.url.includes('/api/auth/')) {
         // Удаляет токен и перенаправляет на /login
       }
       return throwError(() => error);
     })
   );
   ```
   - **`catchError`** - оператор RxJS для обработки ошибок
   - Если ошибка 401/403 (не авторизован) и это не запрос на `/api/auth/`:
     - Удаляет токен через `authService.logout()`
     - Перенаправляет на `/login`
   - **`throwError`** - пробрасывает ошибку дальше

#### Регистрация:

- Регистрируется в `main.ts` как `HTTP_INTERCEPTORS`
- Применяется ко всем HTTP запросам автоматически

---

## 🎨 8. COMPONENTS - UI компоненты

### 8.1. login.component.ts - Компонент входа/регистрации

```typescript
@Component({
  selector: 'app-login',
  standalone: true,
  imports: [CommonModule, ReactiveFormsModule, ButtonModule, ...],
  templateUrl: './login.component.html',
  styleUrl: './login.component.css'
})
export class LoginComponent {
  loginForm: FormGroup;
  registerForm: FormGroup;
  isLoginMode = true;
  errorMessage = '';
}
```

#### Аннотации:

- **`@Component`**
  - **`selector: 'app-login'`** - селектор компонента
  - **`standalone: true`** - standalone компонент
  - **`imports`** - импортирует необходимые модули и компоненты
  - **`templateUrl`** - путь к HTML шаблону
  - **`styleUrl`** - путь к CSS файлу

#### Импорты:

- **`CommonModule`** - базовые директивы Angular (`*ngIf`, `*ngFor`)
- **`ReactiveFormsModule`** - реактивные формы
- **`ButtonModule`, `InputTextModule`, etc.** - модули PrimeNG

#### Свойства:

- **`loginForm: FormGroup`** - форма для входа
- **`registerForm: FormGroup`** - форма для регистрации
- **`isLoginMode: boolean`** - режим (вход или регистрация)
- **`errorMessage: string`** - сообщение об ошибке

#### Конструктор:

```typescript
constructor(
  private fb: FormBuilder,
  private authService: AuthService,
  private router: Router
) {
  this.loginForm = this.fb.group({
    username: ['', [Validators.required]],
    password: ['', [Validators.required]]
  });

  this.registerForm = this.fb.group({
    username: ['', [Validators.required]],
    password: ['', [Validators.required, Validators.minLength(3)]],
    fullName: ['', [Validators.required]],
    groupNumber: ['', [Validators.required]],
    variant: [88, [Validators.required, Validators.min(1)]]
  });
}
```

- **`FormBuilder`** - сервис для создания форм
- **`this.fb.group({...})`** - создает группу полей формы
- **`['', [Validators.required]]`** - начальное значение и валидаторы

#### Методы:

**`onSubmit(): void`**
- Обрабатывает отправку формы
- В зависимости от `isLoginMode` вызывает `login()` или `register()`
- При успехе - перенаправляет на `/main`
- При ошибке - показывает сообщение

**`toggleMode(): void`**
- Переключает между режимом входа и регистрации
- Очищает сообщение об ошибке

---

### 8.2. main.component.ts - Главный компонент приложения

```typescript
@Component({
  selector: 'app-main',
  standalone: true,
  imports: [CommonModule, ReactiveFormsModule, ...],
  templateUrl: './main.component.html',
  styleUrl: './main.component.css'
})
export class MainComponent implements OnInit, AfterViewInit, OnDestroy {
  @ViewChild('canvas', { static: false }) canvasRef!: ElementRef<HTMLCanvasElement>;
  
  form: FormGroup;
  results: ResultResponse[] = [];
  user: any;
  errorMessage = '';
  private canvas: HTMLCanvasElement | null = null;
  private ctx: CanvasRenderingContext2D | null = null;
  private currentR = 1;
  private resizeObserver?: ResizeObserver;
}
```

#### Интерфейсы жизненного цикла:

- **`OnInit`** - вызывается после инициализации компонента
- **`AfterViewInit`** - вызывается после инициализации представления
- **`OnDestroy`** - вызывается перед уничтожением компонента

#### Декораторы:

- **`@ViewChild('canvas', { static: false })`**
  - Получает ссылку на DOM элемент с `#canvas` в шаблоне
  - **`static: false`** - элемент доступен после `AfterViewInit`
  - **`ElementRef<HTMLCanvasElement>`** - тип элемента

#### Свойства:

- **`form: FormGroup`** - форма для ввода координат
- **`results: ResultResponse[]`** - массив результатов
- **`user: any`** - данные пользователя
- **`canvas`** - ссылка на canvas элемент
- **`ctx`** - контекст для рисования на canvas
- **`currentR`** - текущее значение R для масштабирования графика

#### Методы жизненного цикла:

**`ngOnInit(): void`**
- Вызывается один раз после создания компонента
- Загружает данные пользователя
- Проверяет токен
- Загружает результаты

**`ngAfterViewInit(): void`**
- Вызывается после инициализации представления (когда DOM готов)
- Инициализирует canvas
- Настраивает ResizeObserver

**`ngOnDestroy(): void`**
- Вызывается перед уничтожением компонента
- Отключает ResizeObserver (очистка ресурсов)

#### Методы работы с Canvas:

**`initCanvas(): void`**
- Инициализирует canvas и контекст
- Устанавливает размер
- Рисует график
- Настраивает обработчик клика

**`setCanvasSize(): void`**
- Устанавливает размер canvas в зависимости от контейнера
- Максимальный размер: 500px

**`setupResizeObserver(): void`**
- Создает ResizeObserver для отслеживания изменения размера
- При изменении размера перерисовывает график

**`setupCanvasClick(): void`**
- Добавляет обработчик клика на canvas
- При клике вычисляет координаты и отправляет точку

**`onCanvasClick(event: MouseEvent): void`**
- Обрабатывает клик на canvas
- Вычисляет координаты точки в системе координат графика
- Обновляет форму
- Отправляет точку на сервер

#### Методы работы с данными:

**`onSubmit(): void`**
- Обрабатывает отправку формы
- Вызывает `submitPoint()`

**`submitPoint(): void`**
- Проверяет валидность формы
- Проверяет аутентификацию
- Отправляет запрос на сервер через `resultService.checkPoint()`
- При успехе - добавляет результат в начало массива и перерисовывает график

**`onRChange(): void`**
- Вызывается при изменении значения R
- Обновляет `currentR`
- Перерисовывает график

**`clearResults(): void`**
- Очищает все результаты через API
- Очищает массив результатов
- Перерисовывает график

**`logout(): void`**
- Выходит из системы
- Перенаправляет на `/login`

**`loadResults(): void`**
- Загружает результаты с сервера
- Обновляет массив результатов
- Устанавливает значение R из первого результата

#### Методы рисования:

**`drawPlot(): void`**
- Главный метод рисования графика
- Очищает canvas
- Рисует область, оси, метки и точки

**`drawArea(centerX, centerY, scale): void`**
- Рисует область попадания (треугольник, круг, прямоугольник)
- Использует полупрозрачную заливку

**`drawAxes(centerX, centerY, width, height): void`**
- Рисует оси X и Y
- Рисует стрелки на осях

**`drawLabels(centerX, centerY, scale): void`**
- Рисует метки на осях (значения R, -R, R/2, -R/2)
- Рисует названия осей (X, Y)

**`drawPoints(centerX, centerY, scale): void`**
- Рисует точки из массива результатов
- Зеленый цвет для "попал", красный для "не попал"

**`formatDate(dateString: string): string`**
- Форматирует дату в локальный формат (русский)

---

## 🔄 Поток выполнения приложения

### 1. Запуск приложения:

1. **`main.ts`** запускает `bootstrapApplication()`
2. Регистрируются провайдеры (Router, HttpClient, Interceptor)
3. Загружается `AppComponent`
4. Отображается `<router-outlet>`

### 2. Навигация на `/login`:

1. Роутер активирует маршрут `/login`
2. Создается `LoginComponent`
3. Инициализируются формы
4. Пользователь вводит данные и отправляет форму

### 3. Вход/Регистрация:

1. `LoginComponent.onSubmit()` вызывает `authService.login()` или `register()`
2. `AuthService` отправляет HTTP запрос
3. `AuthInterceptor` добавляет заголовки (если нужно)
4. Сервер возвращает токен
5. `AuthService.setAuthData()` сохраняет токен в localStorage
6. `LoginComponent` перенаправляет на `/main`

### 4. Навигация на `/main`:

1. Роутер проверяет `authGuard`
2. `authGuard` проверяет токен через `authService.isAuthenticated()`
3. Если токен есть - разрешает доступ
4. Создается `MainComponent`
5. Вызывается `ngOnInit()` - загружаются результаты
6. Вызывается `ngAfterViewInit()` - инициализируется canvas

### 5. Проверка точки:

1. Пользователь вводит координаты или кликает на canvas
2. `MainComponent.submitPoint()` вызывается
3. `ResultService.checkPoint()` отправляет POST запрос
4. `AuthInterceptor` добавляет `Authorization: Bearer <token>`
5. Сервер проверяет токен и обрабатывает запрос
6. Возвращается результат
7. Результат добавляется в массив
8. График перерисовывается с новой точкой

---

## 🔑 Ключевые концепции Angular

### 1. **Standalone Components**
- Компоненты не требуют NgModules
- Импортируют зависимости напрямую через `imports`

### 2. **Dependency Injection (DI)**
- Angular автоматически внедряет зависимости через конструктор
- Пример: `constructor(private http: HttpClient) {}`

### 3. **Reactive Forms**
- Формы создаются программно через `FormBuilder`
- Валидация через `Validators`
- Связь с шаблоном через `[formGroup]` и `formControlName`

### 4. **RxJS Observables**
- Асинхронные потоки данных
- Операторы: `pipe()`, `tap()`, `catchError()`
- Подписка через `.subscribe()`

### 5. **Lifecycle Hooks**
- `ngOnInit` - инициализация
- `ngAfterViewInit` - после инициализации представления
- `ngOnDestroy` - очистка ресурсов

### 6. **ViewChild**
- Получение ссылки на DOM элемент или дочерний компонент
- Доступен после `AfterViewInit`

### 7. **HTTP Interceptors**
- Перехватывают все HTTP запросы/ответы
- Используются для добавления заголовков, обработки ошибок

### 8. **Route Guards**
- Защита маршрутов
- Проверяют условия перед активацией маршрута

### 9. **localStorage**
- Хранилище в браузере
- Сохраняет данные между сессиями
- Используется для хранения токена

### 10. **Canvas API**
- HTML5 Canvas для рисования графиков
- Методы: `getContext()`, `beginPath()`, `fill()`, `stroke()`

---

## 📝 Итоговая схема архитектуры

```
main.ts (Bootstrap)
    ↓
AppComponent (Root)
    ↓
Router (Routes)
    ├── /login → LoginComponent
    └── /main → MainComponent (protected by authGuard)
         ↓
    AuthGuard → проверяет токен
         ↓
    MainComponent
         ├── AuthService → работа с токеном
         ├── ResultService → API запросы
         └── Canvas → рисование графика
              ↓
    AuthInterceptor → добавляет токен к запросам
              ↓
    Backend API
```

---

Это полное объяснение фронтенда! Если нужны уточнения по какому-то конкретному файлу, методу или концепции - спрашивайте!

