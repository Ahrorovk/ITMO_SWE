# Ответы на вопросы к защите лабораторной работы

## Java EE

### 1. Платформа Java EE. Спецификации и их реализации.

**Java EE (Enterprise Edition)** - платформа для разработки корпоративных приложений.

**Основные спецификации:**
- **JPA (Java Persistence API)** - для работы с БД
- **JAX-RS** - для REST веб-сервисов
- **CDI (Contexts and Dependency Injection)** - для dependency injection
- **EJB (Enterprise JavaBeans)** - для бизнес-логики
- **JTA (Java Transaction API)** - для управления транзакциями
- **JMS (Java Message Service)** - для асинхронных сообщений

**Реализации:**
- **WildFly** (бывший JBoss)
- **GlassFish**
- **WebLogic**
- **TomEE**

---

### 2. Принципы IoC, CDI и Location Transparency. Компоненты и контейнеры.

**IoC (Inversion of Control)** - инверсия управления:
- Контроль создания и управления объектами передается контейнеру
- Компоненты не создают зависимости сами, а получают их извне

**CDI (Contexts and Dependency Injection)**:
- Механизм внедрения зависимостей в Java EE
- Управляет жизненным циклом компонентов
- Создает контексты для компонентов

**Location Transparency**:
- Компонент не знает, где физически находится его зависимость
- Зависимость может быть локальной или удаленной (через RMI, JNDI)

**Компоненты и контейнеры:**
- **Компонент** - класс, управляемый контейнером (EJB, Servlet, CDI Bean)
- **Контейнер** - среда выполнения, управляющая компонентами (создание, DI, жизненный цикл)

---

### 3. Управление жизненным циклом компонентов. Дескрипторы развёртывания.

**Жизненный цикл компонентов:**
1. **Создание** - контейнер создает экземпляр
2. **Инициализация** - вызов `@PostConstruct`
3. **Активное использование**
4. **Уничтожение** - вызов `@PreDestroy`

**Дескрипторы развёртывания:**
- **web.xml** - конфигурация веб-приложения
- **ejb-jar.xml** - конфигурация EJB
- **persistence.xml** - конфигурация JPA
- **application.xml** - конфигурация EAR

---

### 4. Java EE API. Виды компонентов. Профили платформы Java EE.

**Виды компонентов:**
- **Servlet** - обработка HTTP запросов
- **JSP/JSF** - представление (UI)
- **EJB** - бизнес-логика
- **JPA Entity** - сущности БД
- **CDI Bean** - компоненты с DI

**Профили:**
- **Java EE Full Profile** - полный набор технологий
- **Java EE Web Profile** - только веб-технологии (Servlet, JSP, JPA, CDI)
- **Java EE Core Profile** - минимальный набор

---

### 5. Компоненты EJB. Stateless & Stateful Session Beans. EJB Lite и EJB Full.

**EJB (Enterprise JavaBeans)** - компоненты для бизнес-логики.

**Stateless Session Bean:**
- Не сохраняет состояние между вызовами
- Может использоваться несколькими клиентами
- Более производительный
- Пример: сервис для расчетов

**Stateful Session Bean:**
- Сохраняет состояние между вызовами
- Привязан к одному клиенту
- Пример: корзина покупок

**EJB Lite vs EJB Full:**
- **EJB Lite** - упрощенная версия (Stateless, Singleton, Timer)
- **EJB Full** - полная версия (+ Stateful, Message-Driven, Remote)

---

### 6. Работа с электронной почтой в Java EE. JavaMail API.

**JavaMail API:**
- `javax.mail.Session` - сессия для отправки почты
- `javax.mail.Message` - сообщение
- `javax.mail.Transport` - отправка сообщения

**Пример:**
```java
Session session = Session.getDefaultInstance(properties);
Message message = new MimeMessage(session);
message.setFrom(new InternetAddress("from@example.com"));
message.setRecipient(RecipientType.TO, new InternetAddress("to@example.com"));
message.setSubject("Subject");
message.setText("Body");
Transport.send(message);
```

---

### 7. JMS. Реализация очередей сообщений. Способы доставки сообщений до клиента. Message-Driven Beans.

**JMS (Java Message Service)** - API для асинхронных сообщений.

**Типы сообщений:**
- **Queue (Point-to-Point)** - один отправитель, один получатель
- **Topic (Publish-Subscribe)** - один отправитель, много получателей

**Способы доставки:**
- **Synchronous** - блокирующая доставка
- **Asynchronous** - неблокирующая (через MessageListener)

**Message-Driven Bean (MDB):**
- EJB компонент, обрабатывающий JMS сообщения
- Асинхронный обработчик сообщений
- Управляется контейнером

---

### 8. Понятие транзакции. Управление транзакциями в Java EE. JTA.

**Транзакция** - атомарная операция (все или ничего).

**ACID свойства:**
- **Atomicity** - атомарность
- **Consistency** - согласованность
- **Isolation** - изоляция
- **Durability** - долговечность

**JTA (Java Transaction API):**
- API для управления транзакциями
- **Container-Managed Transactions (CMT)** - контейнер управляет транзакциями
- **Bean-Managed Transactions (BMT)** - компонент управляет сам

**Аннотации:**
- `@TransactionAttribute(REQUIRED)` - требуется транзакция
- `@TransactionAttribute(REQUIRES_NEW)` - новая транзакция

---

### 9. Веб-сервисы. Технологии JAX-RS и JAX-WS.

**Веб-сервисы** - способ взаимодействия приложений через HTTP.

**JAX-RS (REST):**
- RESTful веб-сервисы
- Использует HTTP методы (GET, POST, PUT, DELETE)
- JSON/XML формат данных
- Аннотации: `@Path`, `@GET`, `@POST`, `@Produces`, `@Consumes`

**JAX-WS (SOAP):**
- SOAP веб-сервисы
- XML формат (WSDL)
- Более строгий контракт
- Используется для интеграции между системами

**Сравнение:**
- **JAX-RS** - проще, легче, для REST API
- **JAX-WS** - сложнее, но более строгий контракт

---

## Spring Platform

### 10. Платформа Spring. Сходства и отличия с Java EE.

**Spring** - фреймворк для разработки Java приложений.

**Сходства:**
- Dependency Injection
- Управление транзакциями
- Работа с БД (JPA)
- REST веб-сервисы

**Отличия:**

| Java EE | Spring |
|---------|--------|
| Стандарт (спецификации) | Фреймворк (реализация) |
| Тяжелый контейнер | Легковесный контейнер |
| Сложная конфигурация (XML) | Простая конфигурация (аннотации) |
| EJB компоненты | POJO компоненты |
| Application Server | Embedded Server (Spring Boot) |

**В нашем проекте:**
- Используем **Spring Boot** - упрощенная версия Spring
- Не нужен Application Server (встроенный Tomcat)
- Конфигурация через аннотации (`@Service`, `@Repository`, `@RestController`)

---

### 11. Модули Spring. Архитектура Spring Runtime. Spring Security и Spring Data.

**Основные модули Spring:**

1. **Spring Core** - IoC контейнер, DI
2. **Spring AOP** - аспектно-ориентированное программирование
3. **Spring MVC** - веб-фреймворк
4. **Spring Data** - упрощение работы с БД
5. **Spring Security** - безопасность
6. **Spring Boot** - автоконфигурация

**Архитектура Spring Runtime:**
```
Application Context (IoC Container)
    ↓
Bean Factory (создание бинов)
    ↓
Dependency Injection
    ↓
AOP Proxy (для транзакций, безопасности)
```

**Spring Security:**
- Аутентификация и авторизация
- Защита от CSRF, XSS
- В нашем проекте: JWT аутентификация, CORS настройка

**Spring Data:**
- Упрощает работу с репозиториями
- Автоматическая генерация запросов из имен методов
- В нашем проекте: `UserRepository`, `ResultRepository`

---

### 12. Реализация IoC и CDI в Spring. Сходства и отличия с Java EE.

**IoC в Spring:**
- **ApplicationContext** - контейнер IoC
- Управляет созданием и жизненным циклом бинов
- Dependency Injection через конструктор, сеттер или поле

**CDI в Spring:**
- Spring использует свой механизм DI (не CDI из Java EE)
- Аннотации: `@Autowired`, `@Component`, `@Service`, `@Repository`
- Scope: `@Singleton`, `@Prototype`, `@Request`, `@Session`

**Сравнение:**

| Java EE CDI | Spring DI |
|-------------|-----------|
| `@Inject` | `@Autowired` |
| `@Named` | `@Component` |
| `@ApplicationScoped` | `@Singleton` |
| `@RequestScoped` | `@RequestScope` |

**В нашем проекте:**
```java
@Service  // Spring компонент
@RequiredArgsConstructor  // Lombok создает конструктор для DI
public class AuthService {
    private final UserRepository userRepository;  // DI через конструктор
    private final PasswordEncoder passwordEncoder;
}
```

---

### 13. Реализация REST API в Java EE и Spring.

**Java EE (JAX-RS):**
```java
@Path("/api/users")
@Stateless
public class UserResource {
    @GET
    @Produces(MediaType.APPLICATION_JSON)
    public List<User> getUsers() {
        // ...
    }
}
```

**Spring (Spring MVC):**
```java
@RestController
@RequestMapping("/api/users")
public class UserController {
    @GetMapping
    public ResponseEntity<List<User>> getUsers() {
        // ...
    }
}
```

**В нашем проекте (Spring):**
```java
@RestController
@RequestMapping("/api/auth")
@RequiredArgsConstructor
public class AuthController {
    private final AuthService authService;
    
    @PostMapping("/login")
    public ResponseEntity<AuthResponse> login(@Valid @RequestBody LoginRequest request) {
        return ResponseEntity.ok(authService.login(request));
    }
}
```

**Отличия:**
- Spring проще в использовании
- Меньше конфигурации
- Лучшая интеграция с другими модулями Spring

---

## React JS

### 14. React JS. Архитектура и основные принципы разработки приложений.

**React** - библиотека для создания UI (не фреймворк).

**Основные принципы:**
1. **Компонентный подход** - приложение состоит из компонентов
2. **Однонаправленный поток данных** - данные идут сверху вниз
3. **Virtual DOM** - виртуальное DOM для оптимизации
4. **Declarative** - описываем что хотим, а не как это сделать

**Архитектура:**
```
App Component
    ├── Header Component
    ├── Main Component
    │   ├── Form Component
    │   └── List Component
    └── Footer Component
```

---

### 15. Компоненты React. State & props. "Умные" и "глупые" компоненты.

**Компоненты:**
- **Functional Component** - функция, возвращающая JSX
- **Class Component** - класс с методами жизненного цикла

**Props (Properties):**
- Данные, передаваемые от родителя к ребенку
- Неизменяемые (immutable)
- Пример: `<User name="John" age={25} />`

**State:**
- Внутреннее состояние компонента
- Изменяемое через `setState()` (класс) или `useState()` (хук)
- Изменение state вызывает перерисовку

**"Умные" (Smart/Container) компоненты:**
- Управляют state
- Работают с данными (API)
- Передают данные "глупым" компонентам

**"Глупые" (Dumb/Presentational) компоненты:**
- Только отображают данные
- Получают данные через props
- Не имеют собственного state (или минимальный)

---

### 16. Разметка страниц в React-приложениях. JSX.

**JSX (JavaScript XML):**
- Синтаксис для описания UI в JavaScript
- Похож на HTML, но это JavaScript
- Компилируется в `React.createElement()`

**Пример:**
```jsx
function Welcome(props) {
  return <h1>Hello, {props.name}!</h1>;
}

// Компилируется в:
React.createElement('h1', null, 'Hello, ', props.name, '!');
```

**Особенности JSX:**
- Используем `className` вместо `class`
- Используем `{}` для JavaScript выражений
- Компоненты должны начинаться с заглавной буквы

---

### 17. Навигация в React-приложениях. ReactRouter.

**React Router:**
- Библиотека для маршрутизации в React
- Позволяет создавать SPA (Single Page Application)

**Основные компоненты:**
- `<BrowserRouter>` - роутер для браузера
- `<Routes>` - контейнер для маршрутов
- `<Route>` - определение маршрута
- `<Link>` - навигационная ссылка
- `<Navigate>` - программная навигация

**Пример:**
```jsx
<BrowserRouter>
  <Routes>
    <Route path="/" element={<Home />} />
    <Route path="/about" element={<About />} />
    <Route path="/users/:id" element={<User />} />
  </Routes>
</BrowserRouter>
```

---

### 18. Управление состоянием интерфейса. Redux.

**Redux:**
- Библиотека для управления глобальным состоянием
- Основан на паттерне Flux

**Принципы:**
1. **Single Source of Truth** - одно хранилище состояния
2. **State is Read-Only** - состояние неизменяемое
3. **Changes via Pure Functions** - изменения через редюсеры

**Архитектура:**
```
Action → Reducer → Store → Component
```

**Основные концепции:**
- **Store** - хранилище состояния
- **Action** - объект, описывающий изменение
- **Reducer** - чистая функция, изменяющая state
- **Dispatch** - отправка action

**Альтернативы:**
- **Context API** - встроенный в React
- **MobX** - реактивное управление состоянием
- **Zustand** - легковесная библиотека

---

## Angular

### 19. Angular: архитектура и основные принципы разработки приложений.

**Angular** - полноценный фреймворк для разработки SPA.

**Архитектура:**
- **Модульная** - приложение состоит из модулей
- **Компонентная** - UI состоит из компонентов
- **Dependency Injection** - встроенный DI контейнер
- **TypeScript** - типизированный JavaScript

**Основные принципы:**
1. **Модульность** - NgModules для организации кода
2. **Компонентность** - переиспользуемые компоненты
3. **Dependency Injection** - автоматическое внедрение зависимостей
4. **Декларативность** - шаблоны описывают UI

**В нашем проекте (Angular 17):**
- Используем **standalone компоненты** (без NgModules)
- Компоненты импортируют зависимости напрямую

---

### 20. Angular: модули, компоненты, сервисы и DI.

**Модули (NgModules):**
- Организуют код приложения
- Объявляют компоненты, сервисы, директивы
- В Angular 17 можно использовать standalone компоненты (без модулей)

**Компоненты:**
```typescript
@Component({
  selector: 'app-user',
  templateUrl: './user.component.html',
  styleUrls: ['./user.component.css']
})
export class UserComponent {
  // логика компонента
}
```

**Сервисы:**
```typescript
@Injectable({
  providedIn: 'root'  // Singleton на все приложение
})
export class UserService {
  // бизнес-логика
}
```

**Dependency Injection:**
```typescript
constructor(
  private userService: UserService,  // DI через конструктор
  private router: Router
) {}
```

**В нашем проекте:**
- `AuthService` - сервис для аутентификации
- `ResultService` - сервис для работы с результатами
- `LoginComponent`, `MainComponent` - компоненты UI

---

### 21. Angular: шаблоны страниц, жизненный цикл компонентов, подключение CSS.

**Шаблоны:**
- HTML с Angular директивами
- Директивы: `*ngIf`, `*ngFor`, `[property]`, `(event)`
- Интерполяция: `{{ expression }}`

**Пример:**
```html
<div *ngIf="user">
  <h1>{{ user.name }}</h1>
  <button (click)="logout()">Выход</button>
</div>
```

**Жизненный цикл компонентов:**
1. `ngOnChanges` - при изменении входных свойств
2. `ngOnInit` - инициализация (один раз)
3. `ngDoCheck` - проверка изменений
4. `ngAfterContentInit` - после инициализации контента
5. `ngAfterContentChecked` - после проверки контента
6. `ngAfterViewInit` - после инициализации представления
7. `ngAfterViewChecked` - после проверки представления
8. `ngOnDestroy` - перед уничтожением

**В нашем проекте:**
```typescript
export class MainComponent implements OnInit, AfterViewInit, OnDestroy {
  ngOnInit(): void {
    // Загрузка данных при инициализации
    this.loadResults();
  }
  
  ngAfterViewInit(): void {
    // Инициализация canvas после создания DOM
    this.initCanvas();
  }
  
  ngOnDestroy(): void {
    // Очистка ресурсов
    if (this.resizeObserver) {
      this.resizeObserver.disconnect();
    }
  }
}
```

**Подключение CSS:**
- `styleUrls: ['./component.css']` - в декораторе компонента
- Глобальные стили в `styles.css`
- Инкапсуляция стилей (по умолчанию включена)

---

### 22. Angular: клиент-серверное взаимодействие, создание, отправка и валидация данных форм.

**Клиент-серверное взаимодействие:**

**HttpClient:**
```typescript
constructor(private http: HttpClient) {}

getUsers(): Observable<User[]> {
  return this.http.get<User[]>('/api/users');
}
```

**В нашем проекте:**
```typescript
// AuthService
login(request: LoginRequest): Observable<AuthResponse> {
  return this.http.post<AuthResponse>(`${this.apiUrl}/login`, request);
}
```

**HTTP Interceptors:**
- Перехватывают запросы/ответы
- В нашем проекте: `AuthInterceptor` добавляет JWT токен

**Создание форм:**

**Reactive Forms:**
```typescript
constructor(private fb: FormBuilder) {
  this.form = this.fb.group({
    username: ['', [Validators.required]],
    password: ['', [Validators.required, Validators.minLength(3)]]
  });
}
```

**Template-driven Forms:**
```html
<form #form="ngForm">
  <input name="username" ngModel required>
</form>
```

**В нашем проекте (Reactive Forms):**
```typescript
// LoginComponent
this.loginForm = this.fb.group({
  username: ['', [Validators.required]],
  password: ['', [Validators.required]]
});
```

**Валидация:**
- Встроенные валидаторы: `Validators.required`, `Validators.minLength()`, `Validators.max()`
- Кастомные валидаторы
- Проверка в шаблоне: `form.get('field')?.invalid && form.get('field')?.touched`

**Отправка данных:**
```typescript
onSubmit(): void {
  if (this.form.valid) {
    this.service.save(this.form.value).subscribe({
      next: (response) => {
        // успех
      },
      error: (err) => {
        // ошибка
      }
    });
  }
}
```

**В нашем проекте:**
```typescript
// MainComponent
submitPoint(): void {
  const request: PointRequest = this.form.value;
  this.resultService.checkPoint(request).subscribe({
    next: (result) => {
      this.results.unshift(result);
      this.drawPlot();
    },
    error: (err) => {
      this.errorMessage = err.error?.message || 'Ошибка';
    }
  });
}
```

---

## Связь с нашим проектом

### Что используется в нашем проекте:

**Backend (Spring Boot):**
- ✅ Spring Framework (IoC, DI)
- ✅ Spring Security (JWT аутентификация)
- ✅ Spring Data JPA (репозитории)
- ✅ REST API (Spring MVC)
- ✅ Управление транзакциями (`@Transactional`)

**Frontend (Angular):**
- ✅ Standalone компоненты
- ✅ Reactive Forms с валидацией
- ✅ HTTP Client для API запросов
- ✅ HTTP Interceptors (JWT токен)
- ✅ Route Guards (защита маршрутов)
- ✅ Lifecycle hooks (ngOnInit, ngAfterViewInit, ngOnDestroy)
- ✅ Dependency Injection (сервисы, Router)

**Что НЕ используется:**
- ❌ Java EE (используем Spring)
- ❌ EJB (используем Spring компоненты)
- ❌ React (используем Angular)

---

## Ключевые отличия нашего проекта

1. **Spring Boot вместо Java EE:**
   - Более легковесное решение
   - Встроенный сервер (Tomcat)
   - Меньше конфигурации

2. **Angular вместо React:**
   - Полноценный фреймворк
   - Встроенный DI
   - TypeScript для типизации

3. **JWT аутентификация:**
   - Stateless (без сессий)
   - Токен хранится в localStorage
   - Передается в заголовке Authorization

---

Этот документ поможет вам подготовиться к защите! Удачи! 🚀

